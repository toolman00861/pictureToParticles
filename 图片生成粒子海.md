目标效果
一张图片，打散成细颗粒度的单色粒子，鼠标靠近时粒子被推开产生涟漪，鼠标离开后粒子缓慢回归原位。

技术选型

渲染：PixiJS（WebGL性能，粒子量大）
像素读取：Canvas2D（PixiJS不直接提供）
框架：Next.js + TypeScript
样式：Tailwind


整体架构
分三个模块：
1. ImageSampler（图片采样器）
负责读取图片像素，输出粒子初始坐标数组。纯函数，和渲染无关。
2. ParticleSystem（粒子系统）
负责管理所有粒子的状态——当前位置、原始位置、速度。每帧根据鼠标位置更新粒子状态。
3. PixiRenderer（渲染器）
负责初始化PixiJS，接收粒子状态，每帧渲染到屏幕。

数据流
图片
  ↓ ImageSampler
粒子坐标数组（originX, originY）
  ↓ 初始化 ParticleSystem
每帧：鼠标坐标输入 → 计算当前位置 → 输出给 PixiRenderer
  ↓
屏幕

粒子数据结构
每个粒子需要记录：
tstype Particle = {
  x: number        // 当前位置
  y: number
  originX: number  // 原始位置（用于回归）
  originY: number
  vx: number       // 速度（做弹性回归用）
  vy: number
}

采样方案

gap = 3，颗粒度细但不过分
只取alpha > 128的像素，透明区域不生成粒子

力学方案
鼠标排斥：

检测半径80px
距离越近，推力越大（用距离反比）
推力方向是鼠标到粒子的方向

回归原位：

用弹簧缓动，不是直接移动
vx += (originX - x) * stiffness
vx *= damping（阻尼，防止无限震荡）
x += vx

这样粒子回归时有弹性感，不是生硬地飞回去。

组件结构
ParticleEffect/
├── index.tsx          // 对外暴露的组件入口
├── usePixi.ts         // PixiJS初始化hook
├── useParticles.ts    // 粒子系统逻辑hook
├── imageSampler.ts    // 图片采样纯函数
└── types.ts           // 类型定义

参数可配置项
做成props传入，方便复用：
tstype Props = {
  imageSrc: string
  gap?: number          // 采样间隔，默认3
  mouseRadius?: number  // 鼠标影响半径，默认80
  stiffness?: number    // 弹簧硬度，默认0.05
  damping?: number      // 阻尼，默认0.85
  particleSize?: number // 粒子大小，默认1.5
  particleColor?: number // 粒子颜色，默认0xffffff
}